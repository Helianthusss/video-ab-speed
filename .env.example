# Bind to localhost by default. Change only when deployment is designed and secured.
AB_HOST=127.0.0.1
AB_PORT=8765

# Runtime data and generated exports. These paths are excluded from Git.
AB_DATA_DIR=./data
AB_OUTPUT_DIR=./outputs
